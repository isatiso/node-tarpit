#!/bin/zsh

# pnpm
alias pi="pnpm install"
alias pb="pnpm -r run build"
alias pt="pnpm -r run test"

# git
alias gs="git status"
alias gd="git diff"
alias gl="git log --oneline -20"

# misc
alias cls="clear"
